# 6. Python program to print the number of elements present in an array

list1 = list(input("Enter the elements of the array (elements separated by commas): ").split(','))

print(f"Number of elements present in the array is : {len(list1)}")
